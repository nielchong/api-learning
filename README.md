# API Learning

For learning to call APIs and displaying the information on a website built using React.js.

The API calls can perform CRUD operations. Current implementation does not work as the API provided has become unreachable, but the code logic holds.